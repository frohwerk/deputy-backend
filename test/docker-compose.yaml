version: '3.1'

services:

  database:
    build: ../database
    environment:
      POSTGRES_USER: test
      POSTGRES_PASSWORD: drowssap

  go-test:
    build: .
    depends_on:
    - database
#    build:
#      context: '../../database'
#    restart: always
#    environment:
#      POSTGRES_USER: 'testuser'
#      POSTGRES_PASSWORD: 'test!password'
#    ports:
#    - 15432:5432
